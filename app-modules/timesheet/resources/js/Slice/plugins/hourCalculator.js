const calculateHours = (date, timeStart, timeEnd) => {
    console.log(date);
    // const d1 = new Date(date + timeStart);
    // const d2 = new Date(date + timeEnd);
    //
    // let  difference = d1.getTime() - d2.getTime();
    //
    // console.log(new Date(difference).getHours()-1);
    // return new Date(difference).getHours()-1 + ' ' + new Date(difference).getMinutes()+ ' ' + new Date(difference).getSeconds();
}

export default calculateHours
